在Scala中，val和var都用于声明变量，但它们之间有着重要的区别。

val是不可变的变量，一旦声明并赋值后，其值就不能被更改。这意味着val是线程安全的，因为它们的值在多线程环境中不会被更改。此外，使用val可以使代码更易于理解和维护，因为您可以确信它们的值不会在其他地方被更改。

var是可变的变量，可以在声明后更改其值。这意味着var是不安全的，因为它们的值可能在多线程环境中被更改。此外，使用var可能会使代码更难理解和维护，因为您无法确保它们的值是否被更改。

因此，除非您需要更改变量的值，否则最好使用val，因为它可以提高代码的可读性和健壮性。
